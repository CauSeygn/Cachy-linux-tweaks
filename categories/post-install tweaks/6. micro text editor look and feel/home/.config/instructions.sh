# These are my configurations for the micro text editor.
# To use these, simply open the terminal and do:

cp -ri micro ~/.config/

# To make the root account use them as well
# (such as when running sudo micro), do:

sudo cp -ri micro /root/.config/