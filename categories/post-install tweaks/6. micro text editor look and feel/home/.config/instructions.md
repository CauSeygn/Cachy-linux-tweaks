# These are my configurations for the micro text editor.
## To use these, simply open the terminal here and do:

```sh
cp -ri micro ~/.config/
```

## To make the root account use them as well (such as when running sudo micro), do:

```sh
sudo cp -ri micro /root/.config/
```