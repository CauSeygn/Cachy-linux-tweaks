# Open the terminal and do:

sudo cp 99-vm-zram-parameters.conf /etc/sysctl.d/99-vm-zram-parameters.conf

# The config will take effect on next boot.
# These are additional tweaks on top of zRAM taken from the arch wiki.
# For info on what these are and do, read the kernel docs.